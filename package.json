{
  "version": "0.1.0",
  "devDependencies": {
    "@types/react": "^18.0.9",
    "@types/react-dom": "^18.0.5",
    "autoprefixer": "^10.4.7",
    "parcel": "^2.6.0",
    "postcss": "^8.4.14",
    "tailwindcss": "^3.0.24"
  },
  "dependencies": {
    "clsx": "^1.1.1",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "alias": {
    "process": {
      "global": "{}"
    }
  },
  "scripts": {
    "start": "parcel index.html",
    "prebuild": "[[ $CI = true ]] && npx pnpm@3 install -r --store=node_modules/.pnpm-store || echo skipping pnpm install",
    "build": "parcel build src/index.html"
  }
}
